# The production Application boots Hilt, the encrypted preference store and the profile
# seeding coroutine. None of that is under test here, and AndroidKeyStore does not exist on
# the JVM, so the tests run against a plain Application instead.
application=android.app.Application
sdk=34
