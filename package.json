{
  "name": "optiqon-voice-rules",
  "private": true,
  "version": "0.0.0",
  "description": "Firestore security rules and their emulator test suite. Not part of the Android build.",
  "type": "module",
  "scripts": {
    "test:deploy-guard": "node --test tests/deploy/*.test.mjs",
    "test:rules": "firebase emulators:exec --project optiqon-voice-rules-test --only firestore \"node --test --test-concurrency=1 tests/rules/*.test.mjs tests/admin/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.2.0",
    "firebase-admin": "^13.10.0",
    "firebase-tools": "^13.29.1"
  }
}
